# Core libraries
watchdog
torch
ollama
whisperx

# FFmpeg is required for pydub to work with various audio formats
# Install it separately using your system's package manager
# For example, on Ubuntu: sudo apt-get install ffmpeg
# On macOS with Homebrew: brew install ffmpeg
# On Windows with Chocolatey: choco install ffmpeg